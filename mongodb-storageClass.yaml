apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: mongodb-storage-class  # Replace with a descriptive name
provisioner: kubernetes.io/hostPath  # Adjust based on your storage provisioner type

# Optional parameters (might vary based on provisioner)
# This example uses hostPath provisioner for local storage
# For other provisioners, refer to their documentation
parameters:
  path: /mongodb-storage-class  # Uncomment if using hostPath
